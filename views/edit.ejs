<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update User</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body class="bg-zinc-900 text-white min-h-screen">

    <div class="p-10">

        <!-- NAV -->
        <div class="mb-10">
            <a href="/read" class="px-6 py-2 bg-green-500 rounded-md inline-block">
                Read Users
            </a>
        </div>

        <!-- CARD -->
        <div class="max-w-xxl bg-zinc-800 p-8 rounded-xl shadow-lg">

            <h1 class="text-3xl tracking-tight mb-6">
                Update User
            </h1>

            <form action="/edit/<%= user._id %>" method="post" class="space-y-5">

                <input
                    class="w-full px-5 py-3 bg-transparent border-2 border-zinc-700 
                           outline-none rounded-md focus:border-yellow-500"
                    name="name"
                    placeholder="Name"
                    type="text"
                    value="<%= user.name %>">

                <input
                    class="w-full px-5 py-3 bg-transparent border-2 border-zinc-700 
                           outline-none rounded-md focus:border-yellow-500"
                    name="email"
                    placeholder="Email"
                    type="text"
                    value="<%= user.email %>">

                <input
                    class="w-full px-5 py-3 bg-transparent border-2 border-zinc-700 
                           outline-none rounded-md focus:border-yellow-500"
                    name="image"
                    placeholder="Image URL"
                    type="text"
                    value="<%= user.image %>">

                <input
                    class="w-full px-5 py-3 bg-yellow-500 rounded-md text-black font-semibold cursor-pointer"
                    type="submit"
                    value="Update User">

            </form>
        </div>

    </div>

</body>
</html>
