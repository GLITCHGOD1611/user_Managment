<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create User</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body class="bg-zinc-900 min-h-screen flex items-center justify-center">

    <!-- Card -->
    <div class="w-full max-w-md bg-zinc-800 text-white rounded-xl shadow-lg p-8">

        <!-- Header -->
        <div class="flex justify-between items-center mb-6">
            <h1 class="text-2xl font-semibold tracking-tight">Create User</h1>
            <a href="/read" class="text-sm px-4 py-2 bg-green-600 hover:bg-green-700 rounded-md">
                Read Users
            </a>
        </div>

        <!-- Form -->
        <form action="/create" method="post" class="space-y-4">

            <input
                type="text"
                name="name"
                placeholder="Full Name"
                class="w-full px-4 py-2 bg-transparent border border-zinc-600 rounded-md outline-none focus:border-blue-500"
                required
            >

            <input
                type="email"
                name="email"
                placeholder="Email Address"
                class="w-full px-4 py-2 bg-transparent border border-zinc-600 rounded-md outline-none focus:border-blue-500"
                required
            >

            <input
                type="text"
                name="image"
                placeholder="Image URL"
                class="w-full px-4 py-2 bg-transparent border border-zinc-600 rounded-md outline-none focus:border-blue-500"
            >

            <button
                type="submit"
                class="w-full mt-4 px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-md font-medium transition"
            >
                Create User
            </button>

        </form>

    </div>

</body>
</html>
